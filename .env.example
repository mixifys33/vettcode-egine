# Add up to 3 OpenRouter API keys for parallel scanning (round-robin load balancing)
OPENROUTER_API_KEY_1=sk-or-v1-your-key-here
OPENROUTER_API_KEY_2=sk-or-v1-your-second-key
OPENROUTER_API_KEY_3=sk-or-v1-your-third-key

# Optional: comma-separated fallback if you prefer one variable
# OPENROUTER_API_KEYS=key1,key2,key3

# Default free models (comma-separated fallback chain)
OPENROUTER_MODELS=openrouter/free,deepseek/deepseek-chat-v3-0324:free,qwen/qwen-2.5-coder-32b-instruct:free

# Site metadata for OpenRouter rankings (optional)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Backend API URL for seller authentication
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000












hi do you see my folder called vettcode-egine well its done and created perfectly but well the desiag for it is not so that interesting at all please help me redesign and arrange it and make it work and look perfectly for its should be that vettcode egine and everything like it has some professional desgins and some poor childlish design like for the login and sign up and some sections of the report section and also the home section too like make it intactive and well professional somethng a billion dollar free opensourec vettcode egine or scanner would be and bahave or llook like and protrated okay and remove some uncessary texts not logics like well restablish it as an opensource version or vettcode a billion dollar brand okay something like that okay if my text in the home section are not that great then make them okay plus add some interactive loading logic and make it transparent indicate or tell the uses how e dont save they codebase or information in our db unless requested to by the user expect for their registration credentials for anlaystics and study cases but their applications data is worked on in the browser meoemry and are deleted once the user reloads the page or restarts the opertaions okay and dont make it look or soun despirate for users okay